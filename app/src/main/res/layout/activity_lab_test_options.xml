<!-- activity_lab_test_options.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <Spinner
        android:id="@+id/test_options_spinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:entries="@array/test_options" />

    <!-- Other UI elements for date, time, file attachment, and save button will be added dynamically based on selection -->
</LinearLayout>
